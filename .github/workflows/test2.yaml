name: Test 2

on:
  push:
    branches: [ asd ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Test Scripts
        run: |
          echo "GitHub Action 테스트를 시작합니다!"
          echo "현재 디렉토리 목록:"
          ls -R
          echo "Node 버전 확인:"
          node -v
          echo "테스트가 성공적으로 완료되었습니다."
